class Schedule {
  Schedule(
      {//required this.title,
        //required this.memo,
        required this.from,
        required this.to,
       // required this.eventColor,
        });

 // String title;
  ///간단한 메모
  //String memo;
  ///시작시간
  DateTime from;
  ///끝나는 시간
  DateTime to;
  ///이벤트 지정시 색상
 // int eventColor;
}